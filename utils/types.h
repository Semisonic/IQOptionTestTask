#ifndef IQOPTIONTESTTASK_TYPES_H
#define IQOPTIONTESTTASK_TYPES_H

#include <vector>
#include <chrono>

using buffer_t = std::vector<unsigned char>;
using chrono_t = std::chrono::system_clock::time_point;

#endif //IQOPTIONTESTTASK_TYPES_H
